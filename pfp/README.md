# pfp/

Drop your profile picture in here and name it `avatar.jpg` (or update
the `pfp` path in `config.json` to match whatever filename you use).

Recommended: a square image, at least 300x300px, under 500KB.
